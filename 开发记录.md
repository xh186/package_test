# Ledger Flutter 安卓迁移开发记录

## 2026-08-12

### 渐进式目标

1. [完成] 勘察现有网页、后端接口和视觉令牌，定义移动端迁移范围。
2. [完成] 建立 Flutter 与 Android 工程骨架，移植移动端主题、数据模型和核心导航。
3. [完成] 建立本地优先缓存、待同步队列和“仅有更新时写回本地”的同步策略。
4. [完成] 接入服务端认证会话、账目/子账本读写，以及离线使用缓存入口。
5. [完成，受环境限制] 进行源码结构与协议检查；等待 Flutter SDK 环境运行静态分析、测试和 APK 构建前验证。

- 完成原项目勘察：确认 Ledger 的总账本、全部账目、子账本、统计、日历与快速记账入口，以及暖纸色 + 钴蓝 + 薄荷/珊瑚/黄色视觉令牌。
- `/Users/furina488/Desktop/android/` 原为空目录，已建立 Flutter 工程源码、分析规则和 README。
- 完成领域模型：`LedgerTransaction`、`LedgerSubBook`，支持 JSON 序列化和汇总计算。
- 完成本地优先数据层：`shared_preferences` 缓存、ETag 记录、待同步操作队列。
- 完成云端适配：读取 `/api/transactions`、`/api/sub-books`，携带 `If-None-Match`，仅在有更新时替换缓存。
- 服务端现状核对：原 C++ API 尚未输出 `ETag`；客户端已兼容回退到 JSON 版本比对，因此当前服务不改动也能避免内容未变化时重写本地缓存。后续可在服务端补充标准 `ETag`，降低响应载荷。
- Android 平台目录已补齐，并保留 Android 默认 HTTPS 网络策略。由于现有后端示例默认 HTTP，联调前需要使用 HTTPS 反向代理；没有启用全局明文流量。
- 完成主界面：总账本汇总、账目列表、搜索/类型/标签筛选、快速记账、编辑/删除、子账本、统计、日历和日账目查看。
- 加入模型层 Flutter 测试样例。
- 完成登录/注册与会话 Cookie 保存。密码仅用于当前请求，应用只保存服务端会话 Cookie；未登录时仍可选择离线查看和编辑本地缓存。
- 修正离线一致性：离线新增记录的后续编辑会合并到同一待同步操作，删除会取消该操作，不产生重复云端记录。
- 修正子账本离线边界：没有正式云端 ID 的临时子账本不能新增账目，避免恢复网络后丢失子账本关联。

### 验证

- 已完成源码结构检查和接口路径核对。
- 当前开发环境未安装 `flutter` / `dart` 命令，暂无法执行 `flutter pub get`、`flutter analyze`、`flutter test` 或 APK 构建；代码按 Dart 3.3+ 与 Flutter Material 3 API 编写。
- 尚未进行真机/模拟器视觉测试；下一步应在具备 Flutter SDK 的环境执行格式化、静态分析、测试和构建前检查。
